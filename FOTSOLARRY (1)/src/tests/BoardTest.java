package tests;

import board.*;
import player.MoveStrategy;
import player.MoveTransition;
import java.util.Random;

import java.util.List;

//import static org.junit.jupiter.api.Assertions.*;

class BoardTest {
	/*
	 * private Tile tile;
	 * 
	 * @Test public void initialBoard() { final Board board = Board.defaultBoard();
	 * assertEquals(board.actualPlayer().getLegalMoves().size(), 3);
	 * assertEquals(board.actualPlayer().getOpponent().getLegalMoves().size(), 3);
	 * assertEquals(board.actualPlayer(), board.yellowPlayer());
	 * assertEquals(board.actualPlayer().getOpponent(),board.redPlayer());
	 * 
	 * }
	 * 
	 * @Test public void isHorizontalWallPlaced(){ tile = new Tile.EmptyTile(0,2,1);
	 * Wall wall = new Wall(tile,true); tile.placeWall(wall,true); }
	 * 
	 * @Test public void isVerticalWallPlaced(){ tile = new Tile.EmptyTile(0,2,1);
	 * Wall wall = new Wall(tile,false); tile.placeWall(wall,false); }
	 * 
	 * @Test public void testAlgebraicNotation() {
	 * assertEquals(BoardUtils.getPositionAtCoord(0), "a9");
	 * assertEquals(BoardUtils.getPositionAtCoord(1), "b9");
	 * assertEquals(BoardUtils.getPositionAtCoord(2), "c9");
	 * assertEquals(BoardUtils.getPositionAtCoord(3), "d9");
	 * assertEquals(BoardUtils.getPositionAtCoord(4), "e9");
	 * assertEquals(BoardUtils.getPositionAtCoord(5), "f9");
	 * assertEquals(BoardUtils.getPositionAtCoord(6), "g9");
	 * assertEquals(BoardUtils.getPositionAtCoord(7), "h9"); }
	 * 
	 * 
	 * @Test public void mem() { final Runtime runtime = Runtime.getRuntime(); long
	 * start, end; runtime.gc(); start = runtime.freeMemory(); Board board =
	 * Board.defaultBoard(); end = runtime.freeMemory();
	 * System.out.println("That took " + (start-end) + " bytes.");
	 * 
	 * }
	 */





}
